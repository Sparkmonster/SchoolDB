<%= form_with(model: student, local: true) do |form| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :student_name %>
  </div>

  <strong> Student name can't be more than 50 characters long unless if the
    student got proper fantasy school permissions. </strong>

  <div class="field">
    <%= form.label :hobby %>
    <%= form.text_field :hobby, id: :student_hobby %>
  </div>

  <strong> Students' hobby can't be less than 5 characters or greater than 72 characters. </strong>

  <div class="field">
    <%= form.label :major %>
    <%= form.text_field :major, id: :student_major %>
  </div>

  <strong> Student major listings can't be less than 3 characters nor greater than 51 characters due
    to degree requirements and specific certification document sizes. </strong>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age, id: :student_age %>
  </div>

   <strong> Any fantasy creature applying must be 15 or older to join fantasy universities. </strong>

  <div class="field">
    <%= form.label :professor_id %>
    <%= form.select(:professor_id, options_from_collection_for_select(Professor.all, :id, :name)) %>
  </div>
  <strong> All fantasy students must have an accompanying professor per course registration. </strong>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.select(:course_id, options_from_collection_for_select(Course.all, :id, :name)) %>
  </div>

    <strong> All fantasy students must select a course in order to begin active registration. </strong>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
